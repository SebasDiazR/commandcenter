"use client";
// SaveIndicator is now inlined into the top bar of BDCommandCenter.
// This stub is kept so any future isolated use compiles cleanly.
export default function SaveIndicator(_props: {
  dirty: boolean;
  lastSaved: string | null;
  onSave: () => void;
  onUndo: () => void;
  onRedo: () => void;
  canUndo: boolean;
  canRedo: boolean;
}) {
  return null;
}
